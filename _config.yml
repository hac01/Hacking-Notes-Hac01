theme: jekyll-theme-hacker
baseurl: /Hacking-Notes-Hac01
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

defaults:
  -
    scope:
      path: "" # empty string for all files
      type: "posts" # can also be "pages"
    values:
      layout: "default"
